#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string followed by a newline
 * @str: input string
 * Return: always 0 succesful
 */

void _puts(char *str)
{
	puts(str);

	return (0);
}
